const menuButton = document.getElementById("menuButton");
const header = document.querySelector("header");

menuButton.addEventListener("click", toggleMenu);
const navLinks = document.querySelectorAll("#pageNavigation a");

function toggleMenu() {}
